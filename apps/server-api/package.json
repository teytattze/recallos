{
  "name": "@repo/server-api",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun build src/index.ts --outdir ./dist --target bun",
    "dev": "bun --watch src/index.ts",
    "lint": "oxlint",
    "test": "bun test --randomize --pass-with-no-tests"
  },
  "dependencies": {
    "@repo/server-database": "workspace:*",
    "@repo/server-ingestion-core": "workspace:*",
    "@repo/server-ingestion-inbound-adapter": "workspace:*",
    "@repo/server-ingestion-outbound-adapter": "workspace:*",
    "@repo/server-kernel": "workspace:*",
    "@repo/server-platform": "workspace:*",
    "convict": "catalog:",
    "hono": "catalog:"
  },
  "devDependencies": {
    "@repo/typescript-config": "workspace:*",
    "@types/convict": "catalog:"
  }
}
